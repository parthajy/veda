[build]
  base = "veda-web"
  command = "npm run build"
  publish = "dist"

# SPA routing for React Router (deep links like /seller, /t/123)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
