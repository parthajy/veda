import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.useveda.app",
  appName: "Veda",
  webDir: "dist",
  // DO NOT add bundledWebRuntime (not supported in this Capacitor version typing)
};

export default config;
